<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <link rel="stylesheet" href="{{asset('assets/css/bootstrap.min.css')}}" >

        {% block stylesheets %}

        {% endblock %}
        <script defer src="{{asset('assets/js/bootstrap.bundle.min.js')}}"></script>
        {% block javascripts %}

        {% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href={{ path('acceuil') }}>Acceuil</a>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
      {% if app.user %}
        <a class="nav-link" href= {{ path('app_logout') }}>Logout</a>
        </li>
              <li class="nav-item active">
      <a class="nav-link" href={{path('user_emprunts')}}>Liste des emprunts</a>
      </li>
        {% else %}
<a class="nav-link" href= {{ path('login') }}>Login</a>
</li>
      {% endif %}
        
      

      </ul>
             {% if is_granted('ROLE_ADMIN') %}
<div class="dropdown">
  <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
    Pages d'administration
  </a>

  <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
    <li><a class="dropdown-item" href={{path('app_admin_auteur')}}>Gestion des auteur</a></li>
    <li><a class="dropdown-item" href={{path('app_admin_book')}}>Gestion des livres</a></li>
    <li><a class="dropdown-item" href={{path('app_admin_emprunt')}}>Gestion des emprunts</a></li>
        <li><a class="dropdown-item" href={{path('app_admin_emprunteur')}}>Gestion des emprunteurs</a></li>
    <li><a class="dropdown-item" href={{path('app_admin_genre')}}>Gestion des genres</a></li>
    <li><a class="dropdown-item" href={{path('app_admin_user')}}>Gestion des utilisateurs</a></li>
  </ul>
</div>
      {% endif %}

  </div>
</nav>
        {% block body %}
                
            {% endblock %}

    </body>
</html>
